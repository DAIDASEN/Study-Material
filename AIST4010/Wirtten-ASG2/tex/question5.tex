% Question 5 (LeNet on Fashion-MNIST)
	extbf{Setup.} We implement LeNet-5 in PyTorch and evaluate on Fashion-MNIST. Per the clarification, we focus on implementing two networks with different numbers of convolutional layers (in addition to a baseline). Optimizer: Adam with defaults from class (\(\alpha{=}0.001\), \(\beta_1{=}0.9\), \(\beta_2{=}0.999\), \(\epsilon{=}10^{-7}\)).

	extbf{Results.} The table below is auto-generated after running the script. If not yet available, run the Python file noted at the end.

\medskip
\noindent\IfFileExists{generated/question5_results_table.tex}{%
	\input{generated/question5_results_table.tex}
}{Pending: please run the script to generate results.}

\medskip
	extbf{Best result (this run).} \emph{Less CONV (single conv)} achieved \textbf{84.39\%} test accuracy, outperforming the baseline (78.53\%) and the deeper variant (77.16\%). Under a short training budget (5 epochs), the shallower model optimizes faster and generalizes better on Fashion-MNIST.

\medskip
	extbf{Reproduction.} From the \texttt{tex} folder, run: \texttt{python question5.py}. This will download data (if absent), run all experiments, and write outputs under \texttt{tex/generated/}.
