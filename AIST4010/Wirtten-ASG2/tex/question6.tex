% Question 6 (Adam vs. SGD for Logistic Regression)
\textbf{Task.} Implement Adam in NumPy and compare its convergence speed with SGD (lr=$10^{-4}$) on a synthetic binary classification task.

\textbf{Result.} The plot and brief summary are auto-generated after running the script.

\medskip
\noindent\IfFileExists{generated/convergence_comparison.png}{%
	\begin{figure}[h]
		\centering
		\includegraphics[width=0.75\linewidth]{generated/convergence_comparison.png}
	\end{figure}
}{Pending: please run the script to generate the convergence plot.}

\medskip
\noindent\IfFileExists{generated/question6_summary.tex}{\input{generated/question6_summary.tex}}{}

\medskip
\textbf{Observation (this run).} Adam achieved a markedly lower final training loss (\(\approx 0.195\)) than SGD with lr=$10^{-4}$ (\(\approx 0.668\)), confirming faster and stronger convergence under the default Adam hyperparameters.

\medskip
\textbf{Reproduction.} From the \texttt{tex} folder, run: \texttt{python question6.py}. Outputs will be written under \texttt{tex/generated/}.
