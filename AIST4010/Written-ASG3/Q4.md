4. In this problem, we are going to use RNN/GRU/Attention to fit *sin* function. Considering the complexity of optimizing RNN/GRU/Attention, you are allowed to use the PyTorch package. Essentially, you can use NumPy and PyTorch for this problem.

   a. Please generate 10,00 points using the *sin* function. You can generate the 10,00 points from the 0 to 100 as the *x* values, with the step size as 0.1, and apply the *sin* function to get the y values. Then, add the gaussian noise to the y values. You can decide the parameters of the noise by yourselves. Please plot the dataset. (6 pts)

   b. Our goal is to predict the future y values. So, let's use the previous 10 y values as the input to the models. For example, to predict y₁₁, we will use y₁, ..., y₁₀. Please use all the data from the previous sub-problem to train an RNN, a GRU, an Attention, and a logistic regression. For simplicity, please just use one layer for the deep learning models. You can decide the number of nodes by yourselves. You may use mean squared error as the loss function. Please plot the training loss function curve. You should train every model for at least **20 epochs** and you are required to use **GPUs** to train these models. (18 pts) (Hint: You may find the output of logistic regression cannot model the value range of *sin* function. Think about how you could solve it.)

   c. From y₉₉₁, ..., y₁₀₀₀, please generate 200 future points. Notice that, the prediction of one time step will be the input of the future time steps. Please plot out the prediction from RNN/GRU/Attention/logistic regression and report the prediction time. Please try to do some **hyper-parameter tuning** (epochs, learning rate, batch size) to make at least the predictions from RNN looks like a *sin* wave (10 pts)