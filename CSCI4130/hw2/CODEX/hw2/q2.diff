diff --git a/hw2/files/hw2-q2/admin.php b/hw2/files/hw2-q2-fixed/admin.php
index 6db4833..657ad0d 100644
--- a/hw2/files/hw2-q2/admin.php
+++ b/hw2/files/hw2-q2-fixed/admin.php
@@ -21,10 +21,10 @@ if ($_SERVER['REQUEST_METHOD'] === 'GET') {
 // Handle POST actions (book management operations)
 // -------------------------------------------------------
 if ($_SERVER['REQUEST_METHOD'] === 'POST') {
-    // Verify that the user is authenticated before processing
-    if (!is_logged_in()) {
-        http_response_code(401);
-        die('Authentication required.');
+    // Verify that the user is authenticated and authorized as admin
+    if (!is_logged_in() || !is_admin()) {
+        http_response_code(403);
+        die('Admin privileges required.');
     }
 
     // Validate CSRF token to prevent cross-site request forgery
diff --git a/hw2/files/hw2-q2/index.php b/hw2/files/hw2-q2-fixed/index.php
index f1a2279..10f68ad 100644
--- a/hw2/files/hw2-q2/index.php
+++ b/hw2/files/hw2-q2-fixed/index.php
@@ -65,7 +65,7 @@ while ($row = $result->fetchArray(SQLITE3_ASSOC)) {
             <?php foreach ($books as $book): ?>
             <tr>
                 <td>
-                    <span title="<?= sanitize_text($book['description']) ?>">
+                    <span title="<?= sanitize_output($book['description']) ?>">
                         <?= sanitize_output($book['title']) ?>
                     </span>
                 </td>
diff --git a/hw2/files/hw2-q2/search.php b/hw2/files/hw2-q2-fixed/search.php
index b4a78c9..b5d495b 100644
--- a/hw2/files/hw2-q2/search.php
+++ b/hw2/files/hw2-q2-fixed/search.php
@@ -19,7 +19,8 @@ if (isset($_GET['q']) && !empty(trim($_GET['q']))) {
         $query .= " AND year >= " . $year_from;
     }
     if (!empty($_GET['year_to'])) {
-        $query .= " AND year <= " . $_GET['year_to'];
+        $year_to = intval($_GET['year_to']);
+        $query .= " AND year <= " . $year_to;
     }
 
     // Sorting ¡ª validate column name against a strict whitelist
